# Disposable search/runtime proof. No retained account or media data is used.
services:
  postgres:
    volumes: !reset []
    tmpfs:
      - /var/lib/postgresql:rw,size=256m
  router:
    ports: !override ["127.0.0.1::4000"]
