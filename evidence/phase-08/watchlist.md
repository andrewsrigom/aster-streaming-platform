# Owned watchlist checkpoint

P08-R07 is implemented; local acceptance passes and protected publication remains pending. [ADR-0032](../../docs/adr/0032-owned-watchlist-visibility.md) records ownership, live Catalog filtering, bounds and recovery. This is a backend capability, not browser save/resume or a complete Phase 08 release.

## Evidence

- [Focused acceptance](watchlist-focused.txt): strict Engagement build, 84/84 tests, changed-file ESLint and 9/9 Router composition tests pass. The compatible four-subgraph schema includes SetWatchlist/Watchlist known operations. Manifest b469b520b8c107c641aeda10a9d0169364f10093ef3484018a91fee7225d0b2b, compared against released main 4082c3a.
- [Real PostgreSQL](watchlist-postgres-diagnostic.jsonl): compiled services/engagement/dist/test/integration/watchlist-postgres.js against pinned PostgreSQL 18.6 in an isolated tmpfs database. Node 24.19.0 tooling, two CPU/2 GiB; database one CPU/384 MiB. Up/down/up and replay, current readiness/grants, same-key and changed-title/action races, missing receipt/event rollback, all 256 memberships and aggregate read, overflow/reclaimed slot, 25 retained/21 hidden rows with two visible pages of two, progress coexistence, unknown-COMMIT recovery and deletion/foreign-owner denial pass. Query plans are small-fixture evidence, not performance/SLO claims. A follow-up scoped container listing found no remaining watchlist SQL fixtures.
- Earlier [first SQL attempt](watchlist-postgres.jsonl) exposed an incorrect test expectation: COMMIT-stage constraint failure is conservatively INDETERMINATE, with independently proven rollback. [Second attempt](watchlist-postgres-current.jsonl) returned UNAVAILABLE at slot 128. The instrumented run above passed without relaxing deadlines or retrying individual commands; its cause is unproven, not attributed to CPU. Raw attempts are retained. Candidate reads use the bounded aggregate to respect the shared SQL adapter's 64-row limit.
- The [first runtime attempt](watchlist-runtime.jsonl) exhausted its 300-second Docker build window before startup or application assertions; exact project 54d1fc99-b95e-4354-a7f4-a5cb22fd41a0 cleanup reports zero resources and no retained-runtime change. The one [cache-assisted attempt](watchlist-runtime-cached.jsonl) passes under the same limits (exit 0), project 3eedf471-d00e-48ac-bd69-c9aff8cfaebc. It proves the entire application flow, initializer replay, correlation/secrets exclusion and anonymous Playback after optional owners stop. Ownership-checked cleanup removes thirteen containers, eight trust volumes and two networks with zero remaining resources. The proof covers add/replay/conflict/pages/current retirement/removal, ownership/revocation, real Catalog metadata and zero Playback writes. No CPU or host diagnosis is part of this work.

## Gates and recovery

The candidate was committed and rebased as 238ae404b756d23c056848b6e67291d8ad57d936 onto main 0401ae3. The original and rebased trees compare equal. All source hashes pass, and `ASTER_SCHEMA_BASE=0401ae3e850add27ad73fe7be12a1672d5a73414 node apps/router/dist/src/main.js --check` passes with the same manifest. Later memory-only closeout does not change executable sources or require another heavy experiment.

The [candidate gate](watchlist-candidate-gate.txt) passes 47/47 tasks (26 cached, 4m48.539s), including 63 platform tests and current schema compatibility. Exact changed executable/schema sources are in [source hashes](watchlist-source.sha256). Command: documented check:changed invocation with Turbo concurrency two, Node 24.19.0 tooling, four GiB/two GiB Node heap. Only documentation/test-runner safety checks and formatting changed after the runtime snapshot, not watchlist behavior; no heavyweight repeat is needed. No unchanged host, media or browser experiment is required. Existing trust/owner transport tests remain supporting evidence: watchlist reuses ADR-0031 unchanged. Browser integration, general entity batching, event delivery and deletion cleanup remain later work.

P08-R06 is DONE: PR 27 is squash-merged at main 0401ae3e850add27ad73fe7be12a1672d5a73414. Protected CI 33190917857, clean final-head confirmation 5455176079 and exact main push 33191946442 pass; both review threads are resolved. The squash tree equals that reviewed tree. Watchlist may now publish. All recovery stashes, latest 416c574be8e3d14154943308efc1ed1f017683d3, have been restored once; never reapply them. Stop additive runtime and retain schema/data for rollback. Empty-state-only down migration is not a normal recovery command.
