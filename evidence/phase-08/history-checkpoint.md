# History read-side checkpoint

P08-R06 is implemented locally, not released. Domain/application, SELECT-only SQL, GraphQL reads and composed Title references are present on dependent feat/p08-history, now rebased onto identical squash main 4082c3a463b50ba4397f080e1b81bc15e03bf140. All 60 Engagement tests, strict build and scoped ESLint pass. [Affected candidate gate](history-candidate-gate.txt) passes 67/67 tasks (48 cached, 1m6.25s). [Current-main gate](history-main-gate.txt) additionally passes 40/40 tasks (29 cached, 1m25.329s), with compatibility checked against 4082c3a463b50ba4397f080e1b81bc15e03bf140. [Exact read-side source hashes](history-source.sha256) identify the unchanged SQL/Docker proof source. Remaining: predecessor-first protected review/release.

[Rebased real PostgreSQL proof](history-postgres-rebased.jsonl) passes the full progress and history scenarios. [Federated Docker reads](history-federated-runtime.jsonl) pass two one-row keyset pages with current Catalog metadata, completion filtering, retired metadata nullability, foreign/anonymous/deleted/revoked denial, page/cursor abuse rejection and zero read writes. Mutation acceptance and optional-failure playback also pass. Command: `node tools/run-engagement-runtime.mjs`, exit 0, pinned Docker/Node runtime; exact project f00b6753-bda0-4e6d-b638-3903300687bc cleaned all 13 containers, seven trust volumes and two networks. No retained data/CPU/media experiment.

PR 26 CI 33180440040 failed in an unchanged Catalog attestation SQL fixture. The [test-only clock correction](catalog-clock.md) is now published as dea0b5d; CI 33181780482 is running. History's d4320f6 stash was restored once after rebase. No production/history runtime source changed in that correction, so the SQL/Docker evidence remains applicable. Do not reapply any old stash or repeat unchanged heavyweight checks.

Twelve focused pagination/ownership/cancellation tests and six GraphQL tests pass. Engagement's interim full suite passed 57 tests before adding two read GraphQL tests. Strict build and changed-area lint pass. The first real SQL attempt exposed an unsupported SET command in the shared adapter; the read now uses only SELECT and rolls back, without broadening that adapter.

[Real PostgreSQL proof](history-postgres.jsonl) passes: 25 retained aggregates, pages 20/5, one SELECT per page, completed history, resumable-only filtering, foreign/deleted-row hiding and zero writes from reads. Normal EXPLAIN ANALYZE uses the history index via bitmap/sort and continue index via ordered scan. These are tiny fixture plans, not production performance claims. Commands: existing compiled progress-postgres verifier against a uniquely labeled PostgreSQL 18.6 tmpfs fixture, Linux Node 24.19.0 tooling; fixture/network removed with exact ownership checks, zero remaining resources. No retained data/CPU test.

Pagination uses PostgreSQL [row comparisons](https://www.postgresql.org/docs/18/functions-comparisons.html#ROW-WISE-COMPARISON) and existing [ordered indexes](https://www.postgresql.org/docs/18/indexes-ordering.html); checked 2026-08-28. Profile/list-bound cursor and live traversal semantics are Aster choices.

PR 26 confirmation 5051921328 found profile-scoped receipt key conflicts and Playback admission isolation. Both were corrected and tested in 736bcdac7f7aeccd3a37b475dde72f67fcd15ace. This dependent branch was rebased onto that head and stash 678ccde78146453011ed7e9941d29afdad26111d restored once. Do not reapply it. Repeat affected checks and complete federated read acceptance before publication, after predecessor release.
