ASTER PHASE 00 RISK-PROPORTIONATE VERIFICATION EVIDENCE
=======================================================

Work item: P00-R06 corrective maintenance
Evidence date: 2026-08-26
Status: IMPLEMENTED
Implementation candidate: 97759171593a2d553079097f4428e106163de13e based on public main 93147accfae07f9cde9ff73b8080f2edd99bed8a
Environment: Windows host; WSL distribution registered as Ubuntu-20.04
Runtime: Node.js 24.19.0; pnpm 11.24.0; Turborepo 2.10.12

PURPOSE AND RETROSPECTIVE
-------------------------

The written governance model already required focused edit checks, an affected
pre-push gate, and a complete merge gate. The affected command was not exposed
by package.json, and the review and heavyweight-evidence stopping rules were not
explicit. P01-R11 therefore accumulated 14 branch commits between released main
e33f90b and closeout head 21bdfcf, including repeated complete gates, public
clones, hosted runs, and review requests for progressively narrower boundaries.

This correction does not weaken requirement, security, data, availability, or
public-contract gates. It makes the promised candidate command executable and
defines when already-passing heavyweight evidence can remain applicable.

IMPLEMENTED CONTRACT
--------------------

- pnpm check and pnpm check:changed use one repository-owned canonical list of
  24 Turbo tasks.
- pnpm check preserves the existing complete repository graph.
- pnpm check:changed adds Turbo affected execution and fixes TURBO_SCM_BASE to
  main and TURBO_SCM_HEAD to HEAD, overwriting inherited caller values.
- Only one --changed and one --force flag are accepted. Unknown or repeated
  narrowing arguments fail before spawning Turbo. Windows invokes pnpm.cmd
  through a fixed cmd.exe /d /s /c boundary because command scripts are not
  directly executable there; every command argument remains repository-owned.
- Child execution has a 15-minute bound, preserves the selected nonzero status,
  and emits bounded errors without reflecting process or environment details.
  A timeout force-terminates the detached POSIX process group or the complete
  Windows process tree, with a five-second bounded close fallback.
- SIGINT and SIGTERM first forward the original signal to the detached POSIX
  group or request non-forced Windows tree termination. A five-second cleanup
  window precedes the existing force path, both wrapper handlers are removed,
  and conventional statuses 130 and 143 remain bounded.
- Task-input-aware affected selection is enabled. Root lint owns application,
  package, service, tool, and worker source; documentation and community checks
  conservatively own every repository path because their scanners can observe
  arbitrary link targets and alternate template locations; security scanning
  also owns every changed repository file.
- Pull-request template duplicate suppression accepts only a non-symbolic
  case-insensitive alias of the canonical entry. A symbolic alternate path that
  resolves to the canonical template remains a file-set violation.
- Work plans now name iteration, candidate, heavyweight-repeat, and review-stop
  gates. The default review cadence is one complete review plus one confirmation.
- One frozen evidenced candidate may use WAITING_EXTERNAL when only named hosted
  CI, review, or merge state remains. The validator permits at most one later
  dependent IN_PROGRESS item, preserves earliest-actionable order, and requires
  predecessor-first publication and release.

FOCUSED AND FAILURE EVIDENCE
----------------------------

$ pnpm toolchain:test
result: PASS
tests: 20 passed, 0 failed

The eight quality-runner tests prove:
- exact full and changed arguments use one canonical task list;
- Windows cmd.exe and POSIX pnpm invocation plus fixed SCM refs;
- rejection of unknown and duplicate flags;
- exact child-status propagation and sanitized spawn failure;
- POSIX group and Windows tree termination plus stable timeout classification;
- original SIGINT/SIGTERM forwarding, bounded force fallback, conventional exit
  status, and listener cleanup;
- manifest scripts, Turbo future flag, and conservative documentation,
  community, and security input ownership remain aligned.

Additional focused results:
- strict root TypeScript check: PASS
- complete type-aware ESLint check: PASS
- deterministic Prettier check: PASS
- documentation check: PASS, 133 documents, 1,903 headings, 328 local links,
  0 violations
- repository-memory check: PASS, 20 queue items and one active P00-R06 item
- CI policy and security checks: PASS, 0 violations/findings
- Git whitespace check: PASS

CANDIDATE GATES
---------------

$ pnpm check:changed
result: PASS
tasks: 31 successful, 31 total; 28 cached
Turbo task time: 1.151 seconds
wrapper elapsed: 2.28 seconds
selection note: package.json and turbo.json are global graph inputs in this
candidate, so the complete graph is the expected conservative selection.

$ pnpm check --force
result: PASS
tasks: 31 successful, 31 total; 0 cached
Turbo task time: 18.413 seconds
wrapper elapsed: 20.04 seconds

Windows-invocation remediation repeat:
- official Node.js process behavior requires .cmd files to use a command
  processor on Windows;
- the fixed cmd.exe /d /s /c boundary executed pnpm.cmd --version successfully
  on the Windows host;
- 16 runner/toolchain tests and 31 of 31 forced repository tasks passed after
  the remediation;
- remediation Turbo task time: 15.013 seconds; wrapper elapsed: 16.16 seconds.

The first native-Windows graph reached Turbo through the new runner and exposed
two pre-existing portability defects in the community verifier: a case-only path
alias was reported as a duplicate of the same canonical file, and its symbolic
fixture required a Windows privilege not available to a normal evaluator. The
candidate now compares resolved canonical paths and skips only the symbolic-link
fixture when Windows returns EPERM; malformed UTF-8 and duplicate-location
assertions remain unconditional. Nine community tests and 31 of 31 forced WSL
tasks pass after the portability remediation; Turbo task time was 16.874 seconds
and wrapper elapsed was 17.86 seconds.

Native-Windows public candidate bd7809235a08edf7a0507f4ecd330fd7e40b4b0e:
- empty HTTPS clone and frozen install: PASS with Node.js 24.19.0, pnpm 11.24.0,
  and 260 packages reused;
- complete pnpm check --force: PASS, 31 of 31 tasks, 0 cached, 41.216
  seconds of Turbo task time;
- the two symbolic-link adverse fixtures were explicitly skipped because the
  unprivileged host returned EPERM; their Linux/WSL executions remain passing;
- high-severity audit, secret scan through the graph, clean Git state, exact
  origin/SHA validation, and recoverable cleanup of every temporary clone: PASS.

Initial review comment 3864234278 correctly found that the original synchronous
timeout could leave Turbo descendants running. The remediation isolates the
gate, kills the POSIX group or Windows tree on timeout, and adds the seventeenth
toolchain test. The remediated WSL graph passes 31 of 31 forced tasks with zero
cache in 18.097 seconds of Turbo task time and 19.32 seconds elapsed.

Intermediate protected run 32987293757 passed every applicable job at b7363ea
after the GitHub Actions service began accepting work during its public incident.
Confirmation review at that head produced two blocking findings:
- comment 3864537199 found that terminal signals could leave the detached gate
  group alive before the 15-minute timeout;
- comment 3864537208 found that a symbolic alternate pull-request template could
  be mistaken for a case-only alias of the canonical entry.

Candidate 4184fa178b43121910b47d1ba8a961f5aa5ba98f remediates both findings in
one batch. The focused WSL suites pass 18 toolchain tests and 10 community tests.
The complete WSL gate passes 31 of 31 forced tasks with zero cache in 24.622
seconds of Turbo task time.

Native-Windows public candidate 4184fa178b43121910b47d1ba8a961f5aa5ba98f:
- empty HTTPS clone and frozen install: PASS with Node.js 24.19.0, pnpm 11.24.0,
  260 packages reused, zero downloaded, and 6.5 seconds install time;
- complete pnpm check --force: PASS, 31 of 31 tasks, 0 cached, 1 minute 11.643
  seconds of Turbo task time;
- 18 toolchain tests pass, including both signal contracts; the four adverse
  symbolic-link fixtures skip on the unprivileged host after Windows returns
  EPERM, while their WSL executions pass;
- high-severity audit reports no known vulnerability; Git remains clean at the
  exact public SHA;
- the resolved origin, SHA, and temporary-root scope were verified before the
  candidate clone was moved to the Windows Recycle Bin.

Evidence replies 3864697962 and 3864698302 are posted and both confirmation
review discussions are resolved. Because these remediations change named
availability and public-contract boundaries, the written stopping rule permits
one final confirmation at the exact closeout head.

The external-wait correction at 19ee632ca40a04730432a682caece66b0d832034
adds the bounded WAITING_EXTERNAL queue contract and two adverse fixtures. The
repository-memory suite passes 13 tests, including one accepted waiting
predecessor with one dependent active item, rejection of multiple waiting items,
and rejection of skipped actionable work. The complete WSL graph passes 31 of
31 forced tasks with zero cache in 14.58 seconds; audit reports no known
vulnerability.

Final confirmation review at exact PR head dd9f282 produced two new blocking
findings:
- comment 3865479921 proved task-input-aware selection could skip documentation
  after an arbitrary linked target changed and skip community validation after
  an alternate pull-request template appeared;
- comment 3865479931 proved wrapper signals used the timeout force path without
  allowing the child tree a cleanup window.

Candidate 97759171593a2d553079097f4428e106163de13e closes both boundaries in one
batch. Documentation and community checks now declare every repository path as
an input. Wrapper signals request graceful tree termination first, wait one
bounded cleanup window, and use force only as the fallback. The 20-test focused
toolchain suite passes, including POSIX and Windows command contracts, graceful
close before fallback, and forced close after the configured grace window.

$ pnpm check:changed
result: PASS
tasks: 31 successful, 31 total; 9 cached
Turbo task time: 11.735 seconds
wrapper elapsed: 13.44 seconds

The first forced repeat correctly failed after a local branch switch because
ignored P01-R05 dist output remained beside the P00 source and exposed tests not
present in this branch. Git confirmed the three targets contained no tracked
files; targeted ignored-output cleanup removed only packages/config/dist,
packages/http-express/dist, and packages/runtime/dist. The clean repeat then
regenerated current-branch output and passed:

$ pnpm check --force
result: PASS
tasks: 31 successful, 31 total; 0 cached
Turbo task time: 23.992 seconds
wrapper elapsed: 25.63 seconds

The prior native-Windows clean graph remains evidence for bootstrap and complete
repository portability, but it does not prove the changed signal path. The exact
candidate instead has injected POSIX and Windows process-tree contract coverage;
an interactive native-Windows interrupt remains an explicit limitation. No
dependency, lockfile, bootstrap, packaging, Docker, product, or durable-data
input changed. Protected CI at the exact candidate remains required.

$ pnpm audit --audit-level=high
result: PASS, no known vulnerabilities
elapsed: 1.01 seconds

PENDING RELEASE EVIDENCE
------------------------

- Protected pull-request workflow at candidate 97759171593a2d553079097f4428e106163de13e.
- Resolution of confirmation discussions 3865479921 and 3865479931.
- Protected squash merge and post-merge main run.

BOUNDARIES AND LIMITATIONS
--------------------------

- No dependency, lockfile, product package behavior, Docker resource, hosted
  resource, durable state, or application architecture boundary changed.
- Affected selection depends on resolvable Git comparison history. If the local
  main ref or history is unavailable, the operator must refresh it or run the
  complete pnpm check gate; an affected result is never a replacement for the
  final work-item acceptance gate.
- The elapsed observations describe one warm local environment and are workflow
  evidence, not general performance benchmarks.
- The changed graceful-interrupt path is contract-tested for Windows command
  selection but has not been exercised through an interactive native-Windows
  console interrupt at the exact candidate.
