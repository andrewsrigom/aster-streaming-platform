# Playback initial-review correction

2026-08-28, feat/p07-playback after candidate 566f6bba5882e297d0cf326fbf9bb931bbd705ee. [PR 24 initial review](https://github.com/andrewsrigom/aster-streaming-platform/pull/24#pullrequestreview-5050175412) found one P07-R09 availability blocker: normal Router startup depended on Identity while the isolated proof removed that dependency. The base graph now requires only trust initialization, Catalog and Playback. The proof no longer replaces any dependency. Whole-profile startup still includes Identity; targeting Router supports anonymous playback without it.

[CI 33162485356](https://github.com/andrewsrigom/aster-streaming-platform/actions/runs/33162485356) passed source, platform, governance and dependency review, then failed the standalone Catalog cleanup's obsolete `volumes.length <= 3` assertion. Its runtime, database recovery and shutdown assertions passed before that failure. Compose now declares five relevant fixture volumes; the corrected ceiling matches the existing exact-name/label/authority/foreign-attachment validator. No cleanup target or authority was broadened beyond those five known project-owned volumes.

## Verification

- Seven focused Router/Catalog policy tests pass, including base startup independence, refusal to re-add Identity and refusal to hide dependency changes in the proof. Cleanup tests cover all five volume identities and foreign attachments.
- [Real base-graph Playback runtime](review-federated-runtime.txt) passes session creation, rights rejection, absent Identity, locked-dependency deadlines/recovery and correlated redacted logs. `pnpm playback:runtime` checker, executed by the Windows Node supervisor against Docker. The runtime topology change invalidated the earlier startup proof, so this one focused repeat was required.
- [Standalone Catalog runtime](review-catalog-runtime.txt) passes initialization/idempotency, restricted reads, excess-privilege/readiness rejection, PostgreSQL outage/recovery and graceful shutdown, then proves exact cleanup with zero resources. Command: `node tools/verify-local-catalog.mjs` using the same supervisor. Cleanup correction justified this repeat; unchanged media/SQL logic was not rerun locally.
- [Corrected candidate gate](review-candidate-gate.txt) passes `pnpm check:changed`: 64/64 tasks, 47 cached, 1m49.595s. Later prose/source-hash closeout uses cheap docs/memory/secret checks only. [Changed source hashes](review-source.sha256) identify the correction over 566f6bb; the earlier core/API evidence remains supporting evidence.

Both fresh Docker projects were removed after exact ownership validation; retained development/media/Windows processes were untouched. No CPU diagnostic, source GET or film encode. No schema/session/domain/credential change; rollback keeps the prior image and all data. Exact-head protected CI and one confirmation review are still required before squash/post-merge release. Local player work (seven passing tests) is saved in stash 2e85504b1739e3192484c37f5af63977b305eec1 for rebase/resume, not lost or reapplied yet.
