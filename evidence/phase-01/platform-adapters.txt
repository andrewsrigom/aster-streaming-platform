Evidence: P01-R07 narrow runtime platform adapters
Status: IMPLEMENTED local candidates; active implementation
Date: 2026-08-26
Source base: 8dff9d8d57572b2eac944ae98406f3da2979682c
Branch: feat/p01-r07-platform-adapters
Environment: Windows host; WSL Ubuntu-20.04 registration with Ubuntu 24.04 userspace; amd64
Runtime: Node.js v24.19.0; pnpm 11.24.0

Requirement and boundary
- Requirement: P01-R07.
- Owner: shared runtime and dependency-adapter infrastructure.
- Product bounded contexts and durable data owners changed: none.
- Planned packages: clock and IDs in @aster/runtime plus separate @aster/postgres, @aster/redis, @aster/broker-kafka, and @aster/object-storage-s3 packages.
- Explicit exclusions: product schema, migration, repository, typed SQL, Redis key or cache policy, product event, outbox, replay, media rights or publication, application service, public endpoint, Docker Compose dependency, dashboard, alert, and SLO.

Activation evidence
- P01-R06 is released through protected squash 8dff9d8d57572b2eac944ae98406f3da2979682c.
- Protected P01-R06 closeout run 33012535152 and exact post-merge run 33012664408 passed every applicable job.
- Branch feat/p01-r07-platform-adapters was created from clean main at the exact released squash.
- The active plan defines one package per operational dependency, repository-owned declarations, finite deadlines and capacity, caller cancellation, stable telemetry, idempotent close, iteration/candidate gates, heavyweight repeat triggers, review stopping rules, and independent removal.

Selection state
- The activation record selected no dependency. The PostgreSQL checkpoint selects exact pg 8.23.0 and @types/pg 8.23.1, the Redis checkpoint selects exact @redis/client 6.2.1, and the S3 checkpoint selects exact @aws-sdk/client-s3 3.1118.0, @aws-sdk/lib-storage 3.1118.0, and @smithy/node-http-handler 4.11.3 after the live evidence below; only the broker client remains unselected.
- Live registry, official documentation, license, engine, install-script, dependency, audit, cancellation, deadline, redaction, shutdown, and removal evidence must precede installation.
- A Kafka client selected in P01-R07 is provisional until P01-R09 confirms bounded lifecycle against a real broker. Failure requires replacement and repetition of the affected adapter gates before Phase 01 closeout.

Current interpretation
- P01-R07 is active. The dependency-free clock/ID, PostgreSQL, and Redis checkpoints are committed; the narrow S3-compatible object-storage adapter is a green local candidate; the broker adapter remains planned.
- @aster/runtime exposes a frozen system clock returning current fresh Date values, a fixed clock returning isolated deterministic Date values, a UUID v4 generator backed by crypto.randomUUID, and a finite deterministic generator that copies a bounded unique safe identifier sequence without invoking accessors.
- Invalid clock and deterministic-ID configuration uses repository-owned cause-free errors with one stable issue. Deterministic sequence exhaustion uses a fixed cause-free error.

Clock and identifier focused evidence
- pnpm --filter=@aster/runtime typecheck: PASS.
- pnpm --filter=@aster/runtime build: PASS; declarations generated.
- pnpm --filter=@aster/runtime test: PASS; 46 of 46 runtime tests, including 7 clock/ID tests.
- New tests cover fresh current instants, immutable fixed time, invalid epoch bounds, distinct RFC 4122 version 4 output, copied deterministic order, explicit exhaustion, empty/unsafe/oversized/duplicate/accessor-backed sequence rejection, zero accessor reads, cause-free errors, and vendor-free declarations.
- pnpm exec eslint packages/runtime: PASS after explicit unknown narrowing for property descriptors.
- pnpm exec prettier --check packages/runtime: PASS.
- pnpm architecture:check: PASS with zero violation.
- pnpm check:changed: PASS; 13 of 13 selected tasks, zero cached, in 10.341 seconds. The gate included runtime build, typecheck, 46 tests, repository lint, format, unused-code, architecture, documentation, memory, community, toolchain, and secret validation.
- No dependency, lockfile, package manifest, Docker resource, service, schema, cache, event, object, hosted setting, or public command changed.

Pending checkpoint evidence
- Clock/ID source commit 2309f94 is the current coherent checkpoint.
- The broker client selection and adapter remain planned. Real PostgreSQL, Redis, and S3 interoperability remain P01-R09 and are not claimed by the controlled-fake and refused-loopback evidence below.

PostgreSQL client selection evidence
- Registry metadata inspected live on 2026-08-26 selected exact pg 8.23.0, published 2026-08-08 under MIT with Node.js >=16 support, no install script, and a 100044-byte unpacked package. Exact @types/pg 8.23.1, published 2026-08-17 under MIT, supplies development-only declarations.
- The exact resolved pg production graph is pg-cloudflare 1.4.0, pg-connection-string 2.14.0, pg-pool 3.14.0, pg-protocol 1.16.0, pg-types 2.2.0, pgpass 1.0.5, pg-int8 1.0.1, postgres-array 2.0.0, postgres-bytea 1.0.1, postgres-date 1.0.7, postgres-interval 1.2.0, split2 4.2.0, and xtend 4.0.2. Its licenses are MIT or ISC and it adds no native build or install-script boundary.
- pnpm install completed on Node.js 24.19.0 with the repository supply-chain policy passing 324 entries. pnpm audit --audit-level=high reports no known vulnerability.
- Official node-postgres pool documentation and exact 8.23.0 source establish finite pool configuration, release(true) destruction, pool counts, pool.end(), per-query query_timeout, and server statement_timeout behavior.
- Native AbortSignal query cancellation remains unresolved in node-postgres issue 2774. Exact 8.23.0 source shows that a client-side Query read timeout can return before a non-pipeline server query has finished. The repository adapter therefore destroys, rather than returns, a reserved client after caller abort, local operation timeout, query read timeout, SQLSTATE 57014, or any unknown probe failure.
- Exit path: @aster/postgres owns the sole pg dependency and exports no pg, Pool, PoolClient, or QueryResult type. Removing the package and its lockfile graph removes the client without changing a product schema or repository because none exists.

PostgreSQL implemented contract
- @aster/postgres accepts only a bounded plain own-data configuration, validates a PostgreSQL URL without reflecting it, caps the pool at 32 connections, and applies finite connection, idle, statement, query, operation, and close deadlines.
- The public slice exposes connect, a fixed SELECT 1 AS aster_probe probe, a bounded process-local pool snapshot, stable sanitized results, one shared close operation with caller-local cancellation, and lifecycle hooks compatible with @aster/runtime. It exposes no general SQL function, query builder, migration, product repository, schema, transaction policy, or credential-bearing error.
- Adapter-owned capacity prevents an unbounded vendor wait queue. A timed-out or cancelled acquisition keeps its slot reserved until the eventual client is destroyed or acquisition fails. Probe cancellation and timeout destroy the client before releasing the slot.
- Dependency telemetry uses only the released finite postgresql/connect and postgresql/probe vocabulary and outcomes. Snapshot counts are bounded process-local observations; no backend metric, dashboard, or SLO is claimed.
- Close rejects an already-aborted owner without starting shutdown, destroys active owned clients, waits settled adapter work, calls pool.end once, returns within its configured caller deadline, and may remain visibly closing until the underlying vendor drain finishes.

PostgreSQL focused evidence
- @aster/postgres typecheck and build: PASS; generated public declarations contain no pg, pg-pool, PoolClient, or QueryResult reference.
- @aster/postgres test: PASS, 11 of 11 tests. Coverage includes hostile/accessor-backed configuration, exact finite pool config, healthy connect/probe, capacity retention during abandoned acquisition, query timeout, caller abort, concurrent close, side-effect-free pre-aborted close, bounded close timeout with later drain, cause-free lifecycle failure, refused-loopback diagnostic, and public declaration isolation.
- Refused-loopback diagnostic on 127.0.0.1:1: PASS with stable JSON {event:postgres_diagnostic_passed, unavailableOutcome:unavailable, close:completed}; no endpoint, credential, vendor error, or stack is emitted.
- Targeted ESLint and Prettier checks: PASS.
- pnpm check:changed: PASS; 37 of 37 selected tasks, 23 cached, in 9.435 seconds of Turborepo time and 10.46 seconds elapsed. The gate includes package build/typecheck/tests plus repository lint, format, unused-code, architecture, documentation, memory, community, toolchain, CI, local-platform, and secret validation.
- The coherent PostgreSQL source commit remains pending at this evidence checkpoint. Real-container and clean-checkout evidence are deferred by the written P01-R07 repeat triggers until all adapter package and lockfile changes stabilize.

PostgreSQL checkpoint commit
- Commit 1ded757 records the selected client, @aster/postgres package, lockfile, architecture documentation, repository memory, and evidence as one coherent checkpoint.

Redis client selection evidence
- Registry metadata inspected live on 2026-08-26 selected exact @redis/client 6.2.1, published 2026-08-11 under MIT with Node.js >=20 support, no install script, a 5211572-byte unpacked package, SHA-512 integrity LzxBY7SIBvvJiyCgcaJZZakE3fJrZZ++i24+EDW9fKpCl68D35uJcKFpZZwCfOoG9WZTbyZlMzMeM0gtOAMU9Q==, and SHA-1 202d6c6bc88f1ed791fc50604d6d792ddf75438b.
- The only required production dependency is cluster-key-slot 1.1.2 under Apache-2.0. Optional peers are @node-rs/xxhash ^1.1.0 and @opentelemetry/api >=1 <2; the native xxhash peer is not installed, and the already selected @opentelemetry/api 1.9.1 satisfies the optional telemetry peer without a duplicate.
- The workspace install completed on Node.js 24.19.0 with two added pure-JavaScript packages, the repository supply-chain policy passing, and no native build or install-script boundary. pnpm audit --audit-level=high reports no known vulnerability.
- Exact 6.2.1 declarations and source expose finite commandsQueueMaxLength, disableOfflineQueue, connectTimeout, reconnectStrategy, AbortSignal command options, command timeout options, close(), and destroy().
- Exact command-queue source removes abort and timeout listeners when a command moves from the write queue to the waiting-for-reply queue. Therefore those options do not prove cancellation of an already written command. Exact connect source also accepts no AbortSignal and can retry according to reconnectStrategy.
- The adapter adds its own connect and operation deadlines. It disables offline queueing, caps both the vendor queue and repository in-flight count, bounds reconnect attempts and delay, and destroys the current client generation after probe abort, timeout, malformed reply, or unknown command failure. A later explicit connect creates a clean generation.
- Exit path: @aster/redis owns the sole @redis/client dependency and exports no RedisClientType, RedisClientOptions, command, key, or vendor error type. Removing the package and lockfile graph removes the client without changing durable state because Redis is non-authoritative and no cache policy exists.

Redis implemented contract
- @aster/redis accepts only a bounded plain own-data URL and lifecycle configuration. It exposes connect, fixed PING probe, bounded availability snapshot, stable sanitized outcomes, one shared close, and @aster/runtime lifecycle hooks.
- The first slice exposes no generic Redis command, key, value, TTL, invalidation, Lua, lease, rate limit, cache-aside behavior, or product state. This keeps later capabilities owned by their contexts and prevents a premature raw-client abstraction.
- One shared bounded connect preserves caller-local cancellation. Automatic reconnect is capped at the selected attempt count and linear bounded delay. Commands are rejected while offline, and excess repository operations are rejected before the vendor queue grows.
- Probe abort or timeout destroys the whole client generation because exact source cannot prove cancellation after write. Shutdown prevents new work, aborts adapter operations, destroys the client once, waits bounded local/background settlements, and reports failed rather than completed if vendor destruction throws.
- Dependency telemetry uses only redis/connect and redis/probe with the released finite outcomes. Error events update only a bounded availability state; endpoint, credential, vendor error, command argument, and reconnect cause are discarded.

Redis focused evidence
- @aster/redis typecheck and build: PASS; generated public declarations contain no @redis/client, RedisClientType, RedisClientOptions, or cluster-key-slot reference.
- @aster/redis test: PASS, 13 of 13 tests. Coverage includes hostile configuration, exact finite client policy, ready connect/probe, shared connect and caller-local cancellation, connect timeout and generation recovery, in-flight capacity, probe abort and explicit recovery, probe timeout, bounded reconnect state, concurrent/idempotent close, pre-aborted close, close timeout, vendor destroy failure, cause-free lifecycle errors, refused-loopback diagnostic, and declaration isolation.
- Refused-loopback diagnostic on 127.0.0.1:1: PASS with stable JSON {event:redis_diagnostic_passed, unavailableOutcome:unavailable, close:completed}; no endpoint, credential, vendor error, or stack is emitted and the subprocess exits within three seconds.
- Targeted typecheck, build, tests, ESLint, Prettier, Knip, architecture, diagnostic, high-severity audit, secret scan, and git diff checks pass.
- pnpm check:changed: PASS; 40 of 40 selected tasks, 27 cached, in 11.562 seconds of Turborepo time and 12.64 seconds elapsed. The first attempt correctly stopped on two continuous synthetic credential URLs; fixtures were changed to construct the redaction canary without resembling committed credentials, and the complete repeated gate passed.
- Commit f507c77 records the coherent Redis checkpoint. Real-container and clean-checkout evidence are deferred by the written P01-R07 repeat triggers until all adapter package and lockfile changes stabilize.

S3 client selection evidence
- Live registry metadata inspected on 2026-08-26 showed @aws-sdk/client-s3 3.1119.0 and @aws-sdk/lib-storage 3.1119.0 had been published less than 24 hours earlier, so the repository release-age policy rejected them. The newest eligible exact pair was 3.1118.0, published on 2026-08-25, under Apache-2.0 with Node.js >=20 support. Exact @smithy/node-http-handler 4.11.3, published on 2026-08-20 under Apache-2.0 with Node.js >=18 support, supplies explicit connection and request deadlines.
- The exact client-s3 and lib-storage packages unpack to 3295245 and 68916 bytes respectively; node-http-handler unpacks to 127830 bytes. Workspace installation on Node.js 24.19.0 added 34 packages, passed the repository supply-chain policy, added no native build boundary, and pnpm audit --audit-level=high reports no known vulnerability.
- The resolved vendor and transport closure uses Apache-2.0, MIT, ISC, BSD-3-Clause, and 0BSD licenses. @aster/object-storage-s3 is the sole direct owner of the three selected packages and generated public declarations contain no AWS SDK or Smithy type.
- Exact lib-storage source defines queueSize multiplied by partSize as the maximum upload-buffer budget, enforces a five-MiB minimum part size, supports an AbortController, and attempts AbortMultipartUpload when leavePartsOnError is false. The adapter fixes finite queue and part sizes, uses leavePartsOnError false, and separates prompt caller cancellation from a bounded background cleanup grace before destroying the retired client generation.
- Exit path: remove @aster/object-storage-s3 and its exact lockfile graph. No product object, media publication state, rights record, CDN policy, or service imports the package in P01-R07.

S3 implemented contract
- @aster/object-storage-s3 validates a bounded own-data endpoint, region, bucket, credentials, telemetry, deadlines, capacity, object size, multipart budget, and fixture prefix without invoking accessors or reflecting secrets. It uses path-style addressing, maxAttempts 1, finite Smithy connection/request timeouts, request and response checksum support, and SHA-256 upload requests.
- The repository-owned API exposes probe, head, streaming write, streaming read into a caller destination, exact fixture-key deletion, bounded snapshot, one shared close with caller-local cancellation, and runtime lifecycle hooks. It exposes no generic SDK client, signed URL, list or prefix delete, publication state, HLS/CDN policy, rights decision, or product object.
- Accepted writes own and destroy their source, validate observed bytes against the declared length, and cap multipart buffering. Reads reject excessive declared or observed size and destroy owned streams. Capacity rejection occurs before extending vendor work.
- Abort or timeout detaches the ambiguous client generation, returns a finite stable result to the caller, allows a bounded grace for SDK multipart-abort cleanup, then destroys the generation. A later operation may use a fresh generation. Close aborts active work, waits adapter settlements and retirements, remains finite even when vendor work ignores cancellation, destroys each generation once, and reports sanitized failure if destruction fails.
- Dependency telemetry uses only object_storage probe/read/write/delete with finite outcomes. Missing head remains a normal not_found result; no endpoint, bucket, key, credential, URL, vendor message, or arbitrary identifier enters telemetry or diagnostic output.

S3 focused evidence
- @aster/object-storage-s3 typecheck and build: PASS; generated public declarations contain no @aws-sdk, @smithy, S3Client, command, or Upload reference.
- @aster/object-storage-s3 test: PASS, 16 of 16 tests. Coverage includes hostile/accessor-backed configuration, exact finite client policy, probe/head/missing objects, bounded multipart write and ownership, invalid/oversized/exact-length mismatch writes, declared/observed bounded reads, capacity rejection, abort/timeout generation retirement and recovery, fixture-only deletion, concurrent/idempotent/caller-local close, finite forced retirement, vendor-destroy failure, cause-free lifecycle errors, refused-loopback diagnosis, and declaration isolation.
- Refused-loopback diagnostic on 127.0.0.1:1: PASS with stable JSON {event:object_storage_diagnostic_passed, unavailableOutcome:unavailable, close:completed}; no endpoint, credential, bucket, key, vendor error, or stack is emitted and the subprocess exits within three seconds.
- Targeted Prettier, ESLint, typecheck, build, tests, diagnostic, Knip, architecture, dependency/license inventory, and high-severity audit pass.
- pnpm check:changed: PASS; 43 of 43 selected tasks, 29 cached, in 15.106 seconds. The gate includes the S3 package and every affected repository lint, format, unused-code, architecture, documentation, memory, community, toolchain, CI, local-platform, and secret validation.
- The coherent S3 source commit remains next. Exact clean-checkout and real-container evidence remain deferred by the written P01-R07 repeat triggers until all adapter packages and lockfile changes stabilize.
- Real S3-compatible authentication, checksum interoperability, multipart abort cleanup, backpressure under representative object sizes, and process-handle exit remain P01-R09 and are not claimed.
